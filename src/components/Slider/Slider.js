import React from "react";

const Slider = ({ lowest, highest, value, setValue }) => {
    // const [lowest, highest, value] = [0, 100, 50];
    console.log(lowest, highest, value, setValue);
    return (
        <div>
            <h2>This is slider</h2>
        </div>
    );
};

export default Slider;
